# Importing ~/.dotfiles/*
source "${HOME}/.dotfiles/exports"
source "${HOME}/.dotfiles/functions"
source "${HOME}/.dotfiles/aliases"
source "${HOME}/.dotfiles/styles"
source "${HOME}/.dotfiles/plugins"

# Importing (optional) ~/.dotfile
import "${HOME}/.dotfile"
